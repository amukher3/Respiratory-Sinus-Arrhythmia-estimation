%#Verifying the bound on the error
%difference using LARS bounds. 
%Only change here is that the signal
%is a linear combination of the two
%signals.

function [] = ...
    verifyingBounds(fittedSignal)

%pass

end
